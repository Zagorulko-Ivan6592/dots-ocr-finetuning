# dots.ocr: Fine-tuning и Inference

Репозиторий для дообучения и использования модели [dots.ocr](https://github.com/rednote-hilab/dots.ocr) для OCR и парсинга документов на русском языке.

## Что сделано

### 1. Подготовка датасета
- Найден и подготовлен датасет PDF документов на русском языке
- Датасет содержит разнообразные документы: таблицы, текст, формулы, изображения

### 2. Дообучение модели dots.ocr
- Дообучение базовой модели `rednote-hilab/dots.ocr` с использованием LoRA (Low-Rank Adaptation)
- Обучение на русскоязычных PDF документах для улучшения качества OCR
- Объединение LoRA адаптеров с базовой моделью для получения единой дообученной модели
- Результат: дообученная модель готова к использованию

### 3. Оценка качества
- Проведена оценка базовой и дообученной модели на различных бенчмарках:
  - **OCRBench** - стандартный бенчмарк для OCR
  - **OCRBench v2** - обновленная версия OCRBench
  - **MWS-Vision-Bench** - русскоязычный бенчмарк для мультимодальных LLM
- Результаты показывают улучшение качества распознавания после дообучения

### 4. Публикация модели
- Дообученная модель опубликована на Hugging Face Hub
- **Модель доступна по адресу:** [normoldaki31/dots-ocr-finetuned](https://huggingface.co/normoldaki31/dots-ocr-finetuned)
- Модель можно использовать напрямую из Hugging Face Hub или скачать локально

### 5. Ноутбуки для инференса
- **`colab_inference_default.ipynb`** - инференс базовой модели dots.ocr
  - Обработка PDF документов
  - Извлечение текста, таблиц, формул
  - Восстановление Markdown и PDF из результатов
  
- **`colab_inference_merged.ipynb`** - инференс дообученной модели
  - Все возможности базовой модели
  - Улучшенная точность на специфических документах после дообучения

## Результаты бенчмарков

После дообучения на русскоязычных PDF документах модель показала улучшение на различных бенчмарках:

### Сравнение базовой и дообученной модели

![Benchmark Comparison](./benchmark_comparison.png)

*График сравнения метрик базовой и дообученной модели. График будет обновлен после завершения всех бенчмарков.*

### Ключевые улучшения

- **OCRBench**: улучшение точности распознавания текста
- **OCRBench v2**: улучшение на обновленном бенчмарке
- **MWS-Vision-Bench**: улучшение на русскоязычных документах

**Детальные результаты** доступны в папке `results/` после запуска бенчмарков.

## Структура репозитория

```
dots-ocr-finetuning/
├── .gitignore                         # Игнорируемые файлы (модели, веса)
├── README.md                          # Основная документация
├── requirements.txt                  # Зависимости Python
├── CREATE_REPOSITORY.md              # Инструкция по созданию репозитория
│
├── colab_inference_default.ipynb     # Инференс базовой модели
├── colab_inference_merged.ipynb      # Инференс дообученной модели
│
├── merge_lora.py                     # Объединение LoRA с базовой моделью
├── compare_benchmarks.py             # Скрипт сравнения метрик
├── compare_benchmarks.ipynb          # Ноутбук для сравнения результатов
│
├── run_benchmark.sh                  # Скрипт запуска одного бенчмарка
├── run_all_benchmarks.sh              # Скрипт запуска всех бенчмарков
├── HOW_TO_RUN_BENCHMARKS.md          # Инструкция по запуску бенчмарков
└── example_benchmark.sh               # Пример запуска бенчмарка
```

**Примечание:** Модели (базовая и дообученная) не включены в репозиторий из-за их размера (~10-15 ГБ).

## Быстрый старт

### 1. Скачивание моделей

**Базовая модель dots.ocr:**

Скачайте базовую модель одним из способов:

**Вариант 1: Hugging Face Hub (рекомендуется)**
```bash
# Используйте официальную модель
MODEL_PATH = "rednote-hilab/dots.ocr"
```

**Вариант 2: ModelScope (для Китая)**
```bash
# Скачайте через ModelScope
python tools/download_model.py --type modelscope
```

**Вариант 3: Ручная загрузка**
- Перейдите на https://huggingface.co/rednote-hilab/dots.ocr
- Скачайте все файлы модели
- Сохраните в папку (например, `./DotsOCR`)

**Дообученная модель:**

Дообученная модель доступна на Hugging Face Hub:
- **HF Hub:** [`normoldaki31/dots-ocr-finetuned`](https://huggingface.co/normoldaki31/dots-ocr-finetuned)
  - Прямая ссылка: https://huggingface.co/normoldaki31/dots-ocr-finetuned
  - Использование: `MODEL_PATH = "normoldaki31/dots-ocr-finetuned"`
  - Модель автоматически скачается при первом использовании
- **Локально:** `/path/to/DotsOCR_finetuned` (если скачали локально)

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Использование базовой модели

1. Откройте `colab_inference_default.ipynb`
2. Установите пути:
   ```python
   # Вариант 1: Из Hugging Face Hub
   BASE_MODEL_PATH = "rednote-hilab/dots.ocr"
   
   # Вариант 2: Локальный путь
   # BASE_MODEL_PATH = "/path/to/DotsOCR"
   
   PDF_PATH = "/path/to/your/document.pdf"
   ```
3. Запустите ячейки по порядку

### 4. Использование дообученной модели

1. Откройте `colab_inference_merged.ipynb`
2. Установите путь к дообученной модели:
   ```python
   # Вариант 1: Из Hugging Face Hub (рекомендуется)
   MODEL_PATH = "normoldaki31/dots-ocr-finetuned"
   
   # Вариант 2: Локальный путь
   # MODEL_PATH = "/path/to/DotsOCR_finetuned"
   ```
3. Запустите ячейки по порядку

## Настройка vLLM для ускоренного инференса

vLLM позволяет ускорить инференс в 2-5 раз по сравнению с HuggingFace Transformers.

### Установка vLLM

**Важно:** vLLM требует совместимых версий PyTorch. Рекомендуется:

```bash
# Для vLLM 0.11.0+ (официальная поддержка dots.ocr)
pip install vllm>=0.11.0

# Или для совместимости с PyTorch 2.7.0 (как в dots.ocr)
pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128
pip install vllm==0.11.0
```

### Запуск vLLM сервера

#### Для базовой модели:

```bash
vllm serve /path/to/DotsOCR \
  --trust-remote-code \
  --async-scheduling \
  --gpu-memory-utilization 0.5 \
  --port 8000
```

#### Для дообученной модели:

**Вариант 1: vLLM 0.11.0+ (рекомендуется, без регистрации)**

```bash
vllm serve /path/to/DotsOCR_finetuned \
  --trust-remote-code \
  --async-scheduling \
  --gpu-memory-utilization 0.5 \
  --port 8000
```

**Вариант 2: vLLM < 0.11.0 (требуется регистрация)**

```bash
# 1. Установить PYTHONPATH
export PYTHONPATH=/path/to/model/directory:$PYTHONPATH

# 2. Зарегистрировать модель в vLLM
# Если модель в папке DotsOCR_finetuned:
MODEL_NAME=$(basename /path/to/DotsOCR_finetuned)
sed -i '/^from vllm\.entrypoints\.cli\.main import main$/a\
from '"$MODEL_NAME"' import modeling_dots_ocr_vllm' `which vllm`

# 3. Запустить сервер
vllm serve /path/to/DotsOCR_finetuned \
  --tensor-parallel-size 1 \
  --gpu-memory-utilization 0.5 \
  --chat-template-content-format string \
  --served-model-name model \
  --trust-remote-code
```

### Проверка работы vLLM сервера

```bash
# Проверка здоровья сервера
curl http://localhost:8000/health

# Откройте в браузере для интерактивной документации
# http://localhost:8000/docs
```

### Использование vLLM в ноутбуке

В ноутбуке установите:
```python
USE_VLLM = True
VLLM_SERVER_IP = "localhost"
VLLM_SERVER_PORT = 8000
VLLM_PROTOCOL = "http"
```

### Запуск vLLM в фоне

```bash
nohup vllm serve /path/to/DotsOCR_finetuned \
  --trust-remote-code \
  --async-scheduling \
  --gpu-memory-utilization 0.5 \
  --port 8000 > vllm.log 2>&1 &

# Просмотр логов
tail -f vllm.log
```

## Объединение LoRA с базовой моделью

Если у вас есть LoRA адаптеры и вы хотите получить единую дообученную модель:

### Использование скрипта:

1. Отредактируйте пути в `merge_lora.py`:
   ```python
   BASE_MODEL_PATH = "/path/to/DotsOCR"
   LORA_PATH = "/path/to/fine_tuned_lora"
   OUTPUT_PATH = "/path/to/DotsOCR_finetuned"
   ```

2. Запустите:
   ```bash
   python merge_lora.py
   ```

### Использование ноутбука:

В `colab_inference_merged.ipynb` есть ячейка "Объединение LoRA с базовой моделью":
- Установите `MERGE_LORA = True`
- Запустите ячейку
- После завершения установите `MERGE_LORA = False`

**Результат:** Объединенная модель будет сохранена и готова к использованию с vLLM или HF Transformers.

## Решение проблем

### Ошибка: "Cannot uninstall networkx"

```bash
pip install --force-reinstall --no-deps networkx
pip install networkx
```

### Ошибка: "ImportError: cannot import name 'persistent_grid'"

Несовместимость версий PyTorch и vLLM. Решение:

**Вариант 1:** Отключить torch.compile
```bash
export VLLM_TORCH_COMPILE=0
vllm serve ...
```

**Вариант 2:** Переустановить PyTorch
```bash
pip uninstall torch -y
pip install torch==2.7.0 --index-url https://download.pytorch.org/whl/cu128
pip uninstall vllm -y
pip install vllm==0.11.0
```

### vLLM не запускается

Используйте HuggingFace Transformers (без vLLM):
```python
USE_VLLM = False
```

Модель будет работать медленнее, но без проблем с vLLM.

### Ошибка: "Free memory on device is less than desired GPU memory utilization"

Уменьшите использование GPU памяти:
```bash
vllm serve ... --gpu-memory-utilization 0.3  # вместо 0.5
```

Или освободите память:
```bash
nvidia-smi  # посмотреть процессы
kill <PID>  # завершить процессы занимающие GPU
```

### Ошибка: "Can't load image processor ... preprocessor_config.json file"

Эта ошибка возникает, если при объединении LoRA не был скопирован файл `preprocessor_config.json`.

**Решение 1:** Скопировать файл вручную:
```bash
cp /path/to/base/DotsOCR/preprocessor_config.json /path/to/DotsOCR_finetuned/
```

**Решение 2:** Перезапустить скрипт объединения (обновленный скрипт копирует этот файл автоматически):
```bash
python merge_lora.py
```

**Решение 3:** Использовать HuggingFace Transformers вместо vLLM:
```python
USE_VLLM = False
```

## Зависимости

См. `requirements.txt` для полного списка зависимостей.

Основные зависимости:
- `torch>=2.1.0` - PyTorch
- `transformers>=4.35.0` - HuggingFace Transformers
- `peft>=0.7.0` - LoRA адаптеры
- `qwen-vl-utils` - утилиты для Qwen-VL
- `pymupdf` - обработка PDF
- `vllm>=0.11.0` - опционально, для ускорения

## Где скачать модели

### Базовая модель dots.ocr

**Официальная модель (рекомендуется):**
- **Hugging Face Hub:** `rednote-hilab/dots.ocr`
  - Ссылка: https://huggingface.co/rednote-hilab/dots.ocr
  - Использование: `MODEL_PATH = "rednote-hilab/dots.ocr"`

**Альтернативные источники:**
- **ModelScope:** https://www.modelscope.cn/models/rednote-hilab/dots.ocr
  - Для пользователей из Китая
  - Скачать: `python tools/download_model.py --type modelscope`

### Дообученная модель

**Hugging Face Hub (рекомендуется):**
- **HF Hub:** [`normoldaki31/dots-ocr-finetuned`](https://huggingface.co/normoldaki31/dots-ocr-finetuned)
  - Прямая ссылка: https://huggingface.co/normoldaki31/dots-ocr-finetuned
  - Использование: `MODEL_PATH = "normoldaki31/dots-ocr-finetuned"`
  - Автоматическая загрузка при первом использовании
  - Размер модели: ~3B параметров, формат: Safetensors, тип: BF16

**Локальная установка:**
- Скачайте модель из Hugging Face Hub:
  ```bash
  huggingface-cli download normoldaki31/dots-ocr-finetuned --local-dir ./DotsOCR_finetuned
  ```
- Или используйте локальный путь, если модель уже скачана
- Убедитесь, что в модели есть файл `preprocessor_config.json` (необходим для vLLM)

## Хранение моделей

⚠️ **Модели НЕ должны быть в GitHub репозитории!**

GitHub имеет ограничения:
- Максимальный размер файла: 100 MB
- Рекомендуемый размер репозитория: до 1 GB

Модели dots.ocr весят ~10-15 GB, что превышает лимиты GitHub.

### Как загрузить свою дообученную модель на Hugging Face Hub:

См. подробную инструкцию в файле [`UPLOAD_TO_HF.md`](UPLOAD_TO_HF.md)

**Краткая инструкция:**
1. **Hugging Face Hub** (рекомендуется):

   **Шаг 1:** Установите и войдите в Hugging Face:
   ```bash
   pip install huggingface_hub
   huggingface-cli login
   # Введите ваш токен из https://huggingface.co/settings/tokens
   ```

   **Шаг 2:** Создайте репозиторий на Hugging Face:
   - Перейдите на https://huggingface.co/new
   - Создайте новый репозиторий (например, `dots-ocr-finetuned`)
   - Выберите тип: "Model"

   **Шаг 3:** Загрузите модель:
   ```bash
   # Убедитесь, что preprocessor_config.json есть в модели!
   # Если нет, скопируйте из базовой модели:
   cp /home/jovyan/kurkin/geo_bench_zagorulko/OCR/DotsOCR/preprocessor_config.json \
      /home/jovyan/kurkin/geo_bench_zagorulko/OCR/DotsOCR_finetuned/
   
   # Загрузите все файлы модели
   huggingface-cli upload your-username/dots-ocr-finetuned \
     /home/jovyan/kurkin/geo_bench_zagorulko/OCR/DotsOCR_finetuned \
     --repo-type model
   ```

   **Шаг 4:** Использование модели из HF Hub:
   ```python
   # В ноутбуке или скрипте
   MODEL_PATH = "your-username/dots-ocr-finetuned"  # вместо локального пути
   
   # Для vLLM
   vllm serve your-username/dots-ocr-finetuned \
     --trust-remote-code \
     --async-scheduling \
     --gpu-memory-utilization 0.5
   ```

3. **Google Drive / облачное хранилище**:
   - Загрузите модели в Drive
   - Смонтируйте в Jupyter Lab при необходимости

4. **Git LFS** (для небольших моделей, но не рекомендуется для 10+ GB):
   ```bash
   git lfs install
   git lfs track "*.safetensors"
   ```

### Что включено в этот репозиторий:
- ✅ Код (ноутбуки `.ipynb`, скрипты `.py`)
- ✅ Конфигурации (`requirements.txt`, `README.md`)
- ✅ Инструкции (`UPLOAD_TO_HF.md`)
- ✅ `.gitignore` (автоматически исключает модели и веса)

### Что НЕ включено (и не должно быть):
- ❌ Модели (файлы `.safetensors`, `.bin`)
- ❌ Веса моделей
- ❌ Большие файлы (>100 MB)

**Все модели нужно скачивать отдельно** (см. раздел "Где скачать модели" выше).

## Оценка через бенчмарки

Для оценки производительности обеих моделей (базовой и дообученной) используйте [lmms-eval](https://github.com/Zagorulko-Ivan6592/lmms-eval).

📖 **Простая инструкция:** См. [`HOW_TO_RUN_BENCHMARKS.md`](./HOW_TO_RUN_BENCHMARKS.md)

### Быстрый старт:

**1. Установите lmms-eval:**
```bash
git clone https://github.com/Zagorulko-Ivan6592/lmms-eval
cd lmms-eval
pip install -e .
```

**2. Запустите vLLM для базовой модели:**
```bash
vllm serve /home/jovyan/kurkin/geo_bench_zagorulko/OCR/DotsOCR \
  --trust-remote-code --async-scheduling \
  --gpu-memory-utilization 0.5 --port 8000
```

**3. В новом терминале запустите оценку:**
```bash
cd github_repo
chmod +x run_benchmark.sh
./run_benchmark.sh ocrbench 8000
```

**4. Для дообученной модели:**
```bash
# Запустите vLLM на порту 8001
vllm serve /home/jovyan/kurkin/geo_bench_zagorulko/OCR/DotsOCR_finetuned \
  --trust-remote-code --async-scheduling \
  --gpu-memory-utilization 0.5 --port 8001

# Запустите оценку
./run_benchmark.sh ocrbench 8001
```

### Доступные бенчмарки:
- `ocrbench` - OCRBench
- `docvqa_val` / `docvqa_test` - DocVQA
- `infovqa_val` / `infovqa_test` - Infographic VQA
- `chartqa` - ChartQA
- `mme` - MME

**Результаты сохраняются в:** `./results/`

### Сравнение результатов базовой и дообученной модели

После запуска бенчмарков для обеих моделей можно сравнить результаты и визуализировать улучшения:

**1. Запустите скрипт сравнения:**
```bash
python compare_benchmarks.py \
  --base_results ./results/dots_ocr_base_* \
  --finetuned_results ./results/dots_ocr_finetuned_* \
  --output ./benchmark_comparison.png
```

**2. Или используйте ноутбук для анализа:**
- Откройте `compare_benchmarks.ipynb`
- Запустите ячейки для визуализации метрик

**Результат:** График сравнения метрик, показывающий улучшения после дообучения.

## Лицензия

Следует лицензии оригинального проекта dots.ocr.
